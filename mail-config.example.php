<?php
/**
 * ШАБЛОН настроек SMTP. Скопируй этот файл в mail-config.php
 * (тоже в корне сайта, рядом с этим файлом) и впиши реальные данные.
 *
 * ВАЖНО: mail-config.php специально добавлен в .gitignore и НЕ должен
 * попадать в GitHub-репозиторий (репозиторий публичный, а пароль от
 * почты — секрет). Создавай/редактируй mail-config.php только через
 * файловый менеджер хостинга или по FTP, напрямую на сервере.
 */

return [
    'smtp_host' => 'smtp.yandex.ru',
    'smtp_port' => 465,          // 465 = SSL, 587 = STARTTLS
    'smtp_secure' => 'ssl',      // 'ssl' или 'tls'
    'smtp_user' => 'a320b@yandex.ru',
    // Пароль от почтового ящика. Если в Яндекс-аккаунте включена
    // двухфакторная аутентификация — нужен ПАРОЛЬ ПРИЛОЖЕНИЯ
    // (Яндекс ID → Пароли и авторизация → Пароли приложений → Почта),
    // обычный пароль от аккаунта тут не сработает.
    'smtp_pass' => 'ВПИШИ_ПАРОЛЬ_ЗДЕСЬ',
    'from_email' => 'a320b@yandex.ru',
    'from_name' => 'НОРМАПЛЮС',
    'to_email' => 'a320b@yandex.ru', // куда слать заявки
];
